/** Node-half loader entry for the file-browser browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the file-browser surface. */
export function apply(): void {}
